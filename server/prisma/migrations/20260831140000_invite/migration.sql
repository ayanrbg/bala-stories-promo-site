-- Одноразовая ссылка для входа: у кого не работает Google, тому админ выдаёт
-- ссылку, а позже — тот же механизм письмом. Храним идентификатор последней
-- выданной ссылки: сверка с ним и делает ссылку одноразовой, поэтому вошедший
-- по ней гасит её, и переслать её дальше уже нельзя.
ALTER TABLE "Participant" ADD COLUMN IF NOT EXISTS "inviteTokenId" TEXT;
