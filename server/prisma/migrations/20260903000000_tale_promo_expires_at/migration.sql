-- Срок действия промокода на сказки: после этого момента код нельзя ввести.
-- NULL = бессрочно, поэтому все заведённые до сих пор коды остаются без срока
-- и продолжают работать как раньше — отдельный UPDATE для них не нужен.
--
-- Уже открытые сказки живут в Fairy (tale_entitlements) и здесь не участвуют:
-- срок закрывает код для новых людей, а не отбирает выданный подарок.
ALTER TABLE "TalePromo" ADD COLUMN "expiresAt" TIMESTAMP(3);
