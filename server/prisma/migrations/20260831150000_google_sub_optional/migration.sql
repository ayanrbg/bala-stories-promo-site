-- Участник может появиться без Google: по одноразовой ссылке от админа или по
-- письму. Тогда googleSub заполнится позже, при первом входе через Google, —
-- поэтому обязательным он быть не может.
-- Уникальность остаётся: в Postgres NULL не конфликтует с NULL, и таких
-- участников может быть сколько угодно.
ALTER TABLE "Participant" ALTER COLUMN "googleSub" DROP NOT NULL;
